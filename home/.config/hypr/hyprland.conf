# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto
source = ~/.config/hypr/bar.conf

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = $HOME/.config/rofi/launch.sh launcher
$powermenu = $HOME/.config/rofi/launch.sh powermenu

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = ~/.config/hypr/scripts/processes.sh restart
exec-once = qs -c noctalia-shell --no-duplicate

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# nvidia related stuff
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia

# electron garbage
env = ELECTRON_OZONE_PLATFORM_HINT,auto

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

misc {
    vrr = true
}

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 0
    gaps_out = 6
    border_size = 2
    allow_tearing = true

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(78b0ffaa)
    col.inactive_border = rgba(4f4f4faa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true
    layout = scrolling
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 2
    rounding_power = 5

    # Change transparency of focused and unfocused windows
    active_opacity = 1
    inactive_opacity = 1

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}


scrolling {
    column_width = 1.0
    explicit_column_widths = 0.5, 0.67, 1.0
    follow_focus = false
    focus_fit_method = 1
    follow_min_visible = 0.0
    direction = right
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    on_focus_under_fullscreen = 0
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true
}

#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    follow_mouse = 1
    sensitivity = 0
    accel_profile = flat
    # @NOTE: Enabling this is not recommended due to potential cursor desynchronization.
    force_no_accel = false
}

debug {
	disable_logs = true
}

cursor {
	no_hardware_cursors = true
	use_cpu_buffer = true
} 

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, W, exec, qs -c noctalia-shell ipc call wallpaper toggle
bind = $mainMod, D, exec, qs -c noctalia-shell ipc call launcher toggle
bind = $mainMod, P, exec, $powermenu
bind = $mainMod, S, exec, ~/.local/bin/dot-screenshot.sh
bind = $mainMod SHIFT, S, exec, ~/.local/bin/dot-screenshot.sh --selection
bind = $mainMod, J, layoutmsg, focus l
bind = $mainMod, K, layoutmsg, focus r
bind = $mainMod SHIFT, J, layoutmsg, swapcol l
bind = $mainMod SHIFT, K, layoutmsg, swapcol r
bind = $mainMod, U, layoutmsg, promote
bind = $mainMod, I, layoutmsg, move +col
bind = $mainMod SHIFT, I, layoutmsg, move -col
bind = $mainMod, O, layoutmsg, colresize +conf
bind = $mainMod SHIFT, O, layoutmsg, colresize -conf
bind = $mainMod SHIFT, R, exec, ~/.config/hypr/scripts/restart-session.sh

# shell
$ipc = qs -c noctalia-shell ipc call

bind = $mainMod SHIFT, W, exec, $ipc settings toggle

# handle alt + tab
bind = ALT, TAB, cyclenext
bind = ALT, TAB, bringactivetotop
bind = ALT SHIFT, TAB, cyclenext, prev
bind = ALT SHIFT, TAB, bringactivetotop

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5

bind = SUPER_SHIFT, F, fullscreenstate, 2 0
bind = $mainMod, F, fullscreenstate, 1 0

# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = , XF86AudioRaiseVolume, exec, $ipc volume increase
bindel = , XF86AudioLowerVolume, exec, $ipc volume decrease
bindl  = , XF86AudioMute, exec, $ipc volume muteOutput
bindel = , XF86MonBrightnessUp, exec, $ipc brightness increase
bindel = , XF86MonBrightnessDown, exec, $ipc brightness decrease

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# # quickshell settings panel should float and never fullscreen
# windowrule = match:class ^org\\.quickshell$, fullscreen 0
# windowrule = match:class ^org\\.quickshell$, maximize 0
# windowrule = match:class ^org\\.quickshell$, float 1
# windowrule = match:class ^org\\.quickshell$, center 1
# windowrule = match:title ^Shell Settings$, size 920 560

# dialogs / popups should float to avoid getting buried in the scroll layout
windowrule = match:title ^(Open File|Save File|Save As|Select File|Choose File|File Upload)$, float 1
windowrule = match:class ^(xdg-desktop-portal-gtk|xdg-desktop-portal-kde|org.kde.polkit-kde-authentication-agent-1|polkit-gnome-authentication-agent-1|lxqt-policykit-agent)$, float 1

# fix xwayland dragging
windowrule {
  name = fix-xwayland-drag
  match:class = ^$
  match:title = ^$
  match:xwayland = true
  match:float = true
  match:fullscreen = false
  match:pin = false
  
  no_focus = on
}

# osu
windowrule {
  name = osu
  match:class = osu\.exe
  
  no_anim = on
  no_focus = on
  opaque = on
  fullscreen = on
  immediate = on
}

# proton games
windowrule {
  name = proton
  match:class = steam_app_.*
  
  fullscreen = on
  no_max_size = on
  opaque = on
  immediate = on
  no_vrr = off
}

# wine stuff
windowrule {
  name = wine
  match:title = .*\.exe.*

  no_max_size = on
  no_vrr = off
  immediate = on
}

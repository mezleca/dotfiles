# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
#
# This config file uses keycodes (bindsym) and was written for the QWERTY
# layout.
#
# To get a config file with the same key positions, but for your current
# layout, use the i3-config-wizard
#

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Inter 12

exec --no-startup-id dunst
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# gaps
gaps inner 10
gaps outer 0

# super
set $mod Mod4

# alt
set $mod1 Mod1

# default apps
set $term kitty
set $file_manager nemo
set $launcher $HOME/.config/rofi/scripts/launcher.sh
set $power_menu $HOME/.config/rofi/scripts/power_menu.sh
set $bar $HOME/.config/polybar/launch.sh

# reload / restart
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# use these keys for focus, movement, and resize directions when reaching for
# the arrows is not convenient
set $up bracketleft
set $down bracketright
set $left semicolon
set $right apostrophe

# terminal
bindsym $mod+Return exec $term

# file manager
bindsym $mod+e exec $file_manager

# launcher
bindsym $mod+d exec $launcher

# power menu
bindsym $mod+p exec $power_menu

# screenshot
bindsym $mod+s exec $HOME/.config/i3/scripts/screenshot.sh
bindsym $mod+Shift+s exec $HOME/.config/i3/scripts/screenshot.sh --selection

# wallpaper
exec --no-startup-id nitrogen --restore

# bar
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# compositor
#exec_always --no-startup-id picom

# other
exec --no-startup-id $HOME/.config/i3/scripts/watch_windows.sh
exec --no-startup-id otd-daemon
exec --no-startup-id nm-applet

# opt: alt tab
exec alttab -bg "#1c1c1c" -frame "#4a6eff" -font "xft:CommitMono Nerd-12" -fg "#ffffff" -d 1

# disable mouse acceleration for any mouse
exec_always for id in $(xinput list | grep "pointer" | cut -d '=' -f 2 | cut -f 1); do xinput --set-prop $id 'libinput Accel Profile Enabled' 0, 1; done

# use Mouse+Mod1 to drag floating windows to their wanted position
floating_modifier $mod

# no windows tiles
for_window [class=".*"] border pixel 3

# class                 border  backgr. text    indicator child_border
client.focused          #4F4F4F #272727 #ffffff #4F4F4F   #78b0ff
client.focused_inactive #4F4F4F #272727 #ffffff #4F4F4F   #212121
client.unfocused        #4F4F4F #272727 #888888 #4F4F4F   #212121
client.urgent           #4F4F4F #272727 #ffffff #4F4F4F   #FFFFFF
client.placeholder      #4F4F4F #0c0c0c #ffffff #000000   #FFFFFF
client.background       #4F4F4F

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# kill focused window
bindsym $mod+q kill

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym Mod1+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# disable everything floating by default
# for_window [window_role=".*"] floating disable

# start portal
exec_always --no-startup-id killall xdg-desktop-portal xdg-desktop-portal-kde xdg-desktop-portal-gtk
exec_always --no-startup-id sleep 1 && /usr/lib/xdg-desktop-portal-kde &
exec_always --no-startup-id sleep 2 && /usr/lib/xdg-desktop-portal &

# ---- STEAM ----
for_window [class="steam"] floating disable
for_window [class="steam" title="Friends List"] floating enable
for_window [class="steam" title="Steam Settings"] floating enable
for_window [class="steam" title="Sign in to Steam"] floating enable
for_window [class="steam" title="^(?!.*Steam).*"] floating enable
for_window [class="steam_proton" title="^$"] move scratchpad
for_window [class="steam_proton" title="BakkesMod"] floating enable
for_window [class="steam_proton" title="BakkesModInjectorCpp"] floating enable

# ---- DISCORD ----
for_window [class="discord"] floating disable
for_window [class="discord" title="Discord Updater"] floating enable
for_window [class="discord" window_role="pop-up"] floating enable

# ---- BROWSERS ----
for_window [class="firefox" instance="Navigator"] floating disable
for_window [class="firefox" instance="Toolkit"] floating enable
for_window [class="firefox" instance="Places"] floating enable
for_window [class="firefox" instance="Alert"] floating enable
for_window [class="Google-chrome" window_role="pop-up"] floating enable
for_window [class="Chromium" window_role="pop-up"] floating enable
for_window [class="Google-chrome" title="Picture in picture"] floating enable

# ---- SYSTEM APPS ----
for_window [class="Thunar" title="File Operation Progress"] floating enable
for_window [class="Nautilus" window_role="dialog"] floating enable
for_window [class="dolphin" window_role="dialog"] floating enable
for_window [class="kcalc"] floating enable
for_window [class="Galculator"] floating enable
for_window [class="pavucontrol"] floating enable

# ---- OSU! (WINE) ----
for_window [class="osu!.exe" title="^osu!"] floating disable, fullscreen enable
for_window [title="Timing and Control Points"] floating enable

# ---- OTHER LAUNCHERS ----
for_window [class="epicgameslauncher"] floating enable
for_window [class="battle.net.exe"] floating enable

# ---- MULTIMEDIA APPS ----
for_window [class="mpv"] floating enable
for_window [class="Vlc" title="Preferences"] floating enable

# ---- IMAGE EDITORS ----
for_window [class="Gimp" window_role="gimp-toolbox"] floating enable
for_window [class="Gimp" window_role="gimp-dock"] floating enable
for_window [class="Gimp" title="GIMP Startup"] floating enable

# ---- RANDOM STUFF ----
for_window [window_role="dialog"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_type="menu"] floating enable
for_window [window_role="transient"] floating enable
for_window [title="About"] floating enable
for_window [title="Preferences"] floating enable
for_window [title="Settings"] floating enable
for_window [title="ark"] floating enable
for_window [title="Ark"] floating enable

bindsym $mod+minus scratchpad show

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws1
